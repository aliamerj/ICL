provider aws {
  source = "hashicorp/aws"
  version = "5.37.0"
  region = "eu-west-1"
  alias = "east"
  allowed_account_ids = ["123456789012"]
  assume_role = { 
    role_arn = "arn:aws:iam::..." 
  }
}

provider random {
  source = "hashicorp/random"
  version = "~> 3.0"
}
